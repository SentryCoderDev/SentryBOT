# Frontend Knowledge

SentryBOT frontend, admin paneli ve durum izleme için basit web arayüzleridir.

## Mevcut
- `admin_ui` modülü: Web yönetim paneli (statik dosyalar)
- Gateway üzerinden servis edilir

Detay: MCP `search_graph(label:"Route")` ile endpoint sorgulama
