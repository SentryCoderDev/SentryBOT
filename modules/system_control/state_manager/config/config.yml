server:
  host: 0.0.0.0
  port: 8093
defaults:
  operational: idle
  emotions: []
persistence:
  type: sqlite
  path: modules/state_manager/data/state.sqlite3
pubsub:
  enabled: true
  keys:
    - operational
    - emotions
